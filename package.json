{
    "name": "courierai",
    "devDependencies": {
        "@anthropic-ai/sdk": "^0.110.0",
        "@axe-core/playwright": "^4.12.1",
        "@eslint/js": "^10.0.1",
        "@google/genai": "^2.10.0",
        "@playwright/test": "^1.61.1",
        "@resvg/resvg-js": "^2.6.2",
        "@types/bun": "1.3.14",
        "@types/chrome": "^0.2.2",
        "@types/node": "^26.1.0",
        "@types/opentype.js": "^1.3.10",
        "@types/turndown": "^5.0.6",
        "eslint": "^10.6.0",
        "eslint-plugin-svelte": "^3.20.0",
        "globals": "^17.7.0",
        "openai": "^6.45.0",
        "opentype.js": "^2.0.0",
        "pdf-lib": "^1.17.1",
        "prettier": "^3.9.4",
        "prettier-plugin-svelte": "^4.1.1",
        "turndown": "^7.2.4",
        "typescript-eslint": "^8.62.1"
    },
    "private": true,
    "scripts": {
        "check": "bun run lint && bun run format && bun run check:tests && bun run check:scripts",
        "check:scripts": "NO_COLOR=1 bunx tsc -p scripts/tsconfig.json",
        "check:tests": "NO_COLOR=1 bunx tsc -p tsconfig.json",
        "lint": "NO_COLOR=1 eslint .",
        "format": "NO_COLOR=1 prettier . --write",
        "format:check": "NO_COLOR=1 prettier . --check",
        "build": "bun scripts/gen-version.ts && bun run check && NO_COLOR=1 bun run --filter courierai_web build && NO_COLOR=1 bun run --filter courierai_ext typecheck && NO_COLOR=1 bun run --filter courierai_ext zip && NO_COLOR=1 bun run --filter courierai_ext build",
        "build:verbose": "BUILD_VERBOSE=1 bun run build",
        "build:web": "bun scripts/gen-version.ts && bun run check && NO_COLOR=1 bun run --filter courierai_web build",
        "deploy:web": "bun scripts/deploy-web.ts",
        "dev:web": "bun scripts/gen-version.ts && bun run check && NO_COLOR=1 bun run --filter courierai_web typecheck && NO_COLOR=1 bun run --filter courierai_web dev",
        "dev:ext": "bun run check && NO_COLOR=1 bun run --filter courierai_ext build",
        "gen:logo": "bun scripts/gen-logo.ts",
        "test": "playwright test"
    },
    "workspaces": [
        "packages/*"
    ]
}
