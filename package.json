{
  "name": "courier-ai",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "check": "NO_COLOR=1 biome check packages/",
    "format": "NO_COLOR=1 biome format packages/ --write && bunx prettier --write \"packages/**/*.svelte\"",
    "build": "bun run check && NO_COLOR=1 bun run --filter courier_web build && NO_COLOR=1 bun run --filter courier_ext build",
    "dev:web": "bun run check && NO_COLOR=1 bun run --filter courier_web typecheck && NO_COLOR=1 bun run --filter courier_web dev",
    "dev:ext": "bun run check && NO_COLOR=1 bun run --filter courier_ext build"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.12",
    "prettier": "^3.8.3",
    "prettier-plugin-svelte": "^3.5.1"
  }
}
