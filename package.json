{
    "name": "airmailai",
    "devDependencies": {
        "@anthropic-ai/sdk": "^0.123.0",
        "@axe-core/playwright": "^4.13.0",
        "@eslint/js": "^10.0.1",
        "@google/genai": "^2.21.0",
        "@playwright/test": "^1.62.1",
        "@resvg/resvg-js": "^2.6.2",
        "@types/bun": "1.4.0",
        "@types/chrome": "^0.2.8",
        "@types/node": "^26.4.1",
        "@types/opentype.js": "^1.3.10",
        "@types/turndown": "^5.0.6",
        "eslint": "^10.9.1",
        "eslint-plugin-svelte": "^3.23.0",
        "globals": "^17.12.0",
        "openai": "^7.10.0",
        "opentype.js": "^2.0.0",
        "pdf-lib": "^1.17.1",
        "prettier": "^3.9.6",
        "prettier-plugin-svelte": "^4.1.1",
        "turndown": "^7.2.4",
        "typescript": "~6.0.3",
        "typescript-eslint": "^8.69.0"
    },
    "license": "Apache-2.0",
    "private": true,
    "scripts": {
        "check": "bun run lint && bun run format && bun run check:tests && bun run check:scripts",
        "check:scripts": "NO_COLOR=1 bunx tsc -p scripts/tsconfig.json",
        "check:tests": "NO_COLOR=1 bunx tsc -p tsconfig.json",
        "lint": "NO_COLOR=1 eslint .",
        "format": "NO_COLOR=1 prettier . --write",
        "format:check": "NO_COLOR=1 prettier . --check",
        "build": "bun scripts/gen-version.ts && bun run check && NO_COLOR=1 bun run --filter airmailai_web build && NO_COLOR=1 bun run --filter airmailai_ext typecheck && NO_COLOR=1 bun run --filter airmailai_ext zip && NO_COLOR=1 bun run --filter airmailai_ext build",
        "build:verbose": "BUILD_VERBOSE=1 bun run build",
        "build:web": "bun scripts/gen-version.ts && bun run check && NO_COLOR=1 bun run --filter airmailai_web build",
        "deploy:web": "bun scripts/deploy-web.ts",
        "dev:web": "bun scripts/gen-version.ts && bun run check && NO_COLOR=1 bun run --filter airmailai_web typecheck && NO_COLOR=1 bun run --filter airmailai_web dev",
        "dev:ext": "bun run check && NO_COLOR=1 bun run --filter airmailai_ext build",
        "gen:logo": "bun scripts/gen-logo.ts",
        "gen:og": "bun scripts/gen-og-card.ts",
        "test": "playwright test"
    },
    "overrides": {
        "typescript": "~6.0.3"
    },
    "workspaces": [
        "packages/*"
    ],
    "dependencies": {
        "playwright-core": "^1.62.1"
    }
}
